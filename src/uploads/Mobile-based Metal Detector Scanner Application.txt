App will be connected through wire or bluetooth with external metal detector - features keeps track of long lat, time,
detection intensity etc. The parameters have to be researched by student, along with the developing a program, which
understands the raw data generated by scanner and converts it to proper analyzable digital data.